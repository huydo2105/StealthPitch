{
    "name": "ndai-escrow",
    "version": "1.0.0",
    "description": "NDAI Escrow Smart Contract for StealthPitch",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:local": "hardhat run scripts/deploy.js --network localhost",
        "deploy:etherlink": "hardhat run scripts/deploy.js --network etherlinkTestnet",
        "verify:etherlink": "hardhat run scripts/verify.js --network etherlinkTestnet",
        "node": "hardhat node"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "dotenv": "^16.3.1"
    }
}